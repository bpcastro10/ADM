{
  "name": "poc-pru-json-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "json-server --watch db.json --port 3000",
    "dev": "json-server --watch db.json --port 3000"
  },
  "devDependencies": {
    "json-server": "^0.17.4"
  }
}
